import 'package:flutter/material.dart';

class AlarmPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    final text = new Text('this is alarm page');
    return new Center(child:text);
  }
}